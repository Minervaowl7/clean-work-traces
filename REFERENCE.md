# 格式清理参考

## 风险分级

### 高置信删除项

- `草稿、工作稿、内部讨论、仅供讨论、不外发、TODO、TBD、TBC、FIXME、待复核、待核实、待补充`。
- `AI生成、AI撰写、本字段含、对应内容、完整核验记录、删除原表、修改说明、调整说明`。
- `证据等级、核验说明、核验截至、主表采用、源CSV行号、复现脚本、统计规则、数据源SHA-256` 等内部工作字段。
- 本机绝对路径、临时目录、用户名、调试日志、脚本报错、软件锁状态。

### 必须人工判断

- `未查得、暂无、尚未、仅针对、不外推、分期未知、不填写、不使用`。
- 法律免责声明、统计口径、适用范围、风险提示。
- 隐藏Sheet/幻灯片、演讲者备注、批注、脚注、尾注、修订和外部链接。

这些内容可能是必要事实，也可能是工作话术。不能靠关键词自动删除。

## DOCX

1. 先检查：正文、表格、文本框、页眉页脚、脚注尾注、批注、修订、隐藏文字、域代码、文档属性。
2. 最终版通常应接受修订并删除审阅批注；来源说明优先转为脚注、尾注或正文引用。
3. 删除空批注引用、孤立书签、隐藏删除文本和工作路径；不要破坏目录、交叉引用和页码域。
4. 清理 `docProps/core.xml` 与自定义属性中的人员、路径和内部项目名，但保留必要标题、主题和公司信息。
5. 验证 ZIP 完整性、段落/表格数量、受保护文本、页数和逐页渲染。

## DOC

1. 不直接对二进制文件做字符串替换。
2. 复制原件，用 Microsoft Word 或 LibreOffice 转为 DOCX；记录转换工具和输出。
3. 按 DOCX 流程清理；如用户必须要 DOC，再从清理后的 DOCX 另存为 DOC。
4. 同时验收转换前后页数、表格、图片、公式和文本框。

## PPTX

1. 检查所有幻灯片、备注页、评论、隐藏页、母版、版式、页外对象、不可见对象、替代文本和文档属性。
2. 删除空占位符、设计说明、素材来源工作话术和编辑提示；正式来源保留为页脚或简洁批注。
3. 隐藏页只有在确认不含唯一内容后才能删除；否则先迁移必要内容。
4. 检查动画顺序、媒体链接、字体替换、图表数据和导出后逐页画面。

## PPT

按 DOC 的转换原则处理：保留原件，先转 PPTX，清理后按用户需要另存回 PPT。

## XLSX/XLSM

1. 检查可见与隐藏/veryHidden Sheet、批注/注释、定义名称、外链、查询、数据连接、公式、错误值、筛选、冻结窗格、打印区域和文档属性。
2. 领导版不得包含核验Sheet、原始融资明细、哈希、源行号、调试列等工作底表；保留单独审计副本。
3. 必要来源批注只保留 `来源：机构/文件/URL`。不要把方法说明塞进单元格或批注。
4. 删除Sheet前确认其公式、图表或名称未被可见Sheet引用；否则先转为稳定值或保留。
5. 门禁：公式错误为零；每个可见Sheet检查行高、列宽、冻结窗格、分页和打印效果。

## XLS

保留原件并转换为 XLSX 清理。若必须交付 XLS，回存后用 Excel 再打开并复查公式、格式和分页。

## PDF

1. PDF用于最终视觉验收，不建议直接做内容清理。
2. 扫描元数据、批注、附件、表单字段、隐藏图层、路径和草稿词。
3. 内容有误时回到 DOCX/PPTX/XLSX 源文件修改，再重新导出 PDF。

## 来源批注标准

合格：

```text
来源：
国家药品监督管理局：https://example.gov.cn/...
公司年报（2025）：https://example.com/report.pdf
```

不合格：

```text
本字段含数据库之外的信息。
对应内容：……
完整核验记录见工作Sheet。
```

## 交付前对抗检查

- 如果领导追问“这句话为什么写在这里”，文档本身能否用正式来源回答？
- 是否仍有只有制作者才理解的列、Sheet、标签、缩写或颜色？
- 是否把“没有查到”悄悄改成了“没有发生”？
- 是否为了显得严谨而加入防御性方法说明，破坏正式体裁？
- 是否只做了XML/结构检查，却没有看实际页面？
